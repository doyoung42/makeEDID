# Normalise line endings in the repository; check out native on Windows.
* text=auto eol=lf

# Binary-ish payloads must never be line-ending converted.
*.png binary
*.jpg binary
*.pdf binary
*.xlsx binary
*.jar binary
*.bin binary
